# Phase 1e Progress

Completed:
- Added relationship editor UX for selected edges:
  - source node
  - target node
  - relationship type
  - add/remove edge controls
- Added presentation mode layout toggle.
- Added guided presenter overlay with step-by-step script and Next/Back controls.
- Added optional auto-actions per presenter step for key actions (load telecom, run deterministic/probabilistic checks).

Files touched:
- `apps/web/src/components/ModelCanvas.tsx`
- `apps/web/src/lib/presenterScript.ts`
- `apps/web/src/app/globals.css`
